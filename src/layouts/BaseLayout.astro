---
import { getCollection } from 'astro:content';
import FilteredPostList from '@/components/FilteredPostList.astro';
import { SITE, navItems } from '@/lib/site';
import { isPublished, sortPosts } from '@/lib/posts';
import '@/styles/global.css';

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const {
  title = SITE.title,
  description = SITE.description,
  image,
} = Astro.props;

const pageTitle = title === SITE.title ? SITE.title : `${title} | ${SITE.title}`;
const canonical = new URL(Astro.url.pathname, SITE.url);
const searchPosts = sortPosts((await getCollection('posts')).filter(isPublished));
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="canonical" href={canonical} />
    <meta property="og:site_name" content={SITE.title} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    {image && <meta property="og:image" content={new URL(image, SITE.url)} />}
    <meta name="twitter:card" content={image ? 'summary_large_image' : 'summary'} />
    <link rel="alternate" type="application/rss+xml" title={`${SITE.title} RSS`} href="/rss.xml" />
  </head>
  <body>
    <a class="skip-link" href="#content">Skip to content</a>
    <header class="site-header">
      <div class="header-inner">
        <a class="brand" href="/" aria-label={`${SITE.title} home`}>
          <span class="brand-mark">P</span>
          <span class="brand-copy">
            <strong>{SITE.title}</strong>
            <small>book notes and reviews</small>
          </span>
        </a>
        <FilteredPostList posts={searchPosts} variant="header" />
        <nav class="site-nav" aria-label="Primary navigation">
          {
            navItems.map((item) => (
              <a href={item.href} aria-current={Astro.url.pathname === item.href ? 'page' : undefined}>
                {item.label}
              </a>
            ))
          }
        </nav>
      </div>
    </header>
    <main id="content">
      <slot />
    </main>
    <footer class="site-footer">
      <div class="footer-copy">
        <strong>{SITE.title}</strong>
        <span>Static, searchable, and markdown-powered.</span>
      </div>
      <nav class="footer-nav" aria-label="Footer navigation">
        <a href="/archive/" aria-current={Astro.url.pathname === '/archive/' ? 'page' : undefined}>Archive</a>
        <a href="/rss.xml">RSS</a>
      </nav>
    </footer>
  </body>
</html>
